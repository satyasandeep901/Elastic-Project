pipeline {
  agent { label 'jenkins-ssh-slave' }
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook -i hosts Elastic-Project/KafkaLogstash-Playbooks-main/kafka.yml'
'
      }
    }
  }
}
