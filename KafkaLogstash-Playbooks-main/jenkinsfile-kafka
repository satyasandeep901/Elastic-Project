pipeline {
  agent { label 'jenkins-ssh-slave' }
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook Elastic-Project/KafkaLogstash-Playbooks-main/kafka.yml'
      }
    }
  }
}
